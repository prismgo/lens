# Prismgo Lens Foundation

- Use Lens MCP tools first when you need project facts, routes, config, schema, logs, or local documentation.
- Treat generated guidance as development-only context; do not import Prismgo Lens into production code.
- Prefer small, explicit Go changes that follow the host application's existing package boundaries.
- Preserve user-authored `.ai` files at the same path; project guidance overrides built-in guidance.
