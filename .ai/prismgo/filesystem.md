# PrismGo Filesystem

- Use the PrismGo filesystem abstraction for storage behavior.
- Keep user input paths normalized and scoped to the intended disk.
- Do not expose absolute machine paths in shared config or user-facing output.
