# PrismGo Schema

- Name tables, columns, indexes, and model fields consistently.
- Add indexes for lookup and join paths that new code actually uses.
- Do not change existing schema semantics to satisfy tests unless the production behavior is wrong.
